apiVersion: v1
kind: Service
metadata:
  name: openfold2-prewarm
  namespace: nim-fast-start
spec:
  selector:
    app.kubernetes.io/name: openfold2
    nim-fast-start.nebius.com/state: active
  ports:
    - name: http
      port: 8000
      targetPort: http
