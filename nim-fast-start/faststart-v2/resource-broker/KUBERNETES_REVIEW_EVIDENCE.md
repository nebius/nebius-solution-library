# Kubernetes v4 replacement review evidence

Recorded 2026-08-19 UTC from isolated branch
`agent/catalog-switch-resource-broker`. Rejected commit
`e58597cfe0b355563d6a5656487201978bf92627` is preserved unchanged as the
direct parent of this replacement. This candidate created no Nebius resource.

## Read-only provider evidence

- Nebius CLI `/usr/local/bin/nebius` is version `0.12.206`; the only frozen
  profile is `sandbox`.
- Frozen caller is `service_account_profile` identity
  `serviceaccount-i00pafr0ydvbaxj952`, parent
  `project-i00xz31gpr00xp9jhp982v`.
- The prior zero-mutation inventory remains canonical: all three authorized
  projects were `ACTIVE` in their expected regions; Kubernetes 1.34, Ubuntu
  24.04, the CPU/H100 presets, and `cuda13.0` were advertised.
- Installed create help/schema was reread without cloud mutation. Cluster and
  node-group create expose no request-correlation token or returned audit
  receipt. Thus a post-intent exact-name object is preserved as foreign/
  ambiguous even if it copied every label and spec.
- The prior complete orphan scan found zero broker-prefixed resources. Manager
  direction forbids another cloud scan or any create before review.

Canonical read-only artifacts:

- `evidence/kubernetes-authorized-inventory-20260819.json`, SHA-256
  `05c0f3b761baf2d9515fc65ab9b90a3be190c3c9e4840861e44624caf8e7243c`.
- `evidence/kubernetes-orphan-scan-review-candidate.json`, SHA-256
  `a16f2bbcb71163d5b9ec5d19da9277efbb906b97622e4036004cc3ba3417accd`.

## Immutable non-admitted candidate

- Lease/backend: `catalog-switch-kubernetes-resource-lease/v4` /
  `nebius-managed-kubernetes/v3`.
- Lease/state: `k8s-baseline-new-node-review2-candidate` / `PLANNED`.
- Project/region: `project-e00z6b02t8ddk96c49` / `eu-north1`.
- Prefix: `mlsp-csw-catalog-switch-k8s-94ac6842`.
- Request SHA-256:
  `94ac6842061009e015c7d82b6281f3e6646941b1e9dc11f5ad39f0adde32e9bc`.
- Plan SHA-256:
  `2a64cdb2913fa6116cfe32a7f23c29203476f1e77bdb05c1c09e90b2299f3dce`.
- Lease file SHA-256:
  `d00ee6fc59889c1098ea7c20875965904158b2d24bd7a2806aaa7fc319e92969`.
- Expected duration/cost: four hours / `$8.940816`.
- TTL/deadline/ceiling: 24 hours / `2026-08-20T15:00:00Z` /
  `$53.644899`; hard cap is `$60.000000`.
- Exact cleanup owner: `catalog-switch-resource-broker`.
- External accepted-event authority: `PENDING_CONSUMER_REVIEW`.
- Private runner network path: `PENDING_CONSUMER_PROOF`.
- `live_creation_gates.admitted` is `false`; cloud IDs, resources, create
  operations, cluster/API server, GPU group IDs, and node IDs are all empty.

The 16 planned graph vertices are supervisor-visible as
`PLAN_ONLY_CREATE_NOT_ADMITTED`: null ID, no create intent, no provider absence
claim. A task-local mode-0600 signing key was generated by offline planning,
ignored by Git, absent from exports, and scheduled for exact unlink after any
future reviewed lifecycle. It is not a cloud resource or execution admission.

## Rejection closure

- Honest post-T0 `NO_PREEMPTIBLE_CAPACITY` now signs and durably persists an
  exact-parent/name provider-list receipt proving zero matches, zero create
  intents, and `create_admitted=false`. `cleanup-attempt` accepts that canonical
  no-create evidence, retains `GPU_CAPACITY_FAILED`, and releases the attempt.
- T0 requires a mode-0600 external receipt signed by a reviewed Ed25519
  recorder/validator authority whose implementation SHA-256 and source commit
  are immutable. A caller-constructed ledger and self-asserted recorder/boot no
  longer establish authority.
- Receipt/event/demand/lease joins cover exact metric-contract SHA-256, trace
  ID, trace-request SHA-256, scenario, full model/version/artifact identity,
  full workload/input identity, and request/attempt/event/ledger identities.
- `common.atomic_json`, kubeconfig staging/promotion, signing-key creation, and
  local-secret deletion fsync the affected parent directory before success.
- An object discovered after an unreceipted create intent cannot be adopted:
  without provider correlation/audit evidence it is marked
  `AMBIGUOUS_FOREIGN_PRESERVED`, never ledgered for deletion, and blocks parent
  cleanup. The adversary copies exact name, labels, parent, timestamp window,
  and spec.
- Every live entry point also requires an immutable reviewed private-runner
  receipt proving task-owned private-subnet access to the internal-only API,
  with no public IP or ingress. The production profile/request intentionally
  leave this pending.
- Prior v3 defenses remain: strict installed provider schemas, private control
  plane, live GPU attestation, signed spec-hashed intents/resource rows,
  exclusive mutation lock, provider-child/replacement reconciliation,
  pre-delete ownership checks, crash-idempotent dependency barriers, canonical
  retry receipts, and truthful supervisor ambiguity.

## Offline execution evidence

- `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -v
  resource-broker/tests`: `46/46 PASS` — eight VM v1 regressions and 38
  Kubernetes/combined-supervisor tests.
- `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -v` from
  `faststart-v2`: `55/55 PASS` for the existing fast-start suites.
- Draft 2020-12 validation passed for the v3 request, v3 demand template, and
  v4 lease. Every resource-broker JSON document parsed; candidate
  `assert_integrity` and the zero-mutation gate passed.
- Python compilation and `git diff --check` passed. The only VM-common code
  change is crash durability for atomic state replacement; all eight VM tests
  remain green.
- The strict fake rejects unknown provider fields. Tests also inspect installed
  CLI help and require `size_bytes`, reject `size_gibibytes`, and verify cluster
  omission of Karpenter/public endpoints.
- New adversaries cover zero-row capacity-miss cleanup and receipt tampering,
  forged external signatures, trace/metric/scenario/full-target mutation,
  pending recorder/runner gates, parent-directory fsync, and exact copied
  name/labels/spec preservation. All prior profile/identity, kubeconfig,
  provider-child, GPU-attestation, concurrent-create, signed-row,
  delete-before-save, dependency-barrier, supervisor-ambiguity, and immutable
  plan adversaries remain green.

## Supervisor and stop condition

The atomic Task Deck ledger at `docs/supervision/resources.json` has schema
`catalog-switch-supervisor-resource-ledger/v2`, four leases, 41 rows, all
manager-required fields, 16 v4 plan-only rows with no absence claim, zero
missing fields, and `contains_secrets=false`. Its sealed SHA-256 is
`b57f005cc224d4309fffb332fa48b8e5bc1f9c18c153f959a68db1ad9de41965`.

No network, subnet, security group, IAM object, registry, bucket, cluster,
control plane, node group, Compute node, GPU, kubeconfig, workload, or model was
created. Live work stays blocked until the baseline consumer provides reviewed
external-validator and private-runner evidence, this lease is replanned, and a
fresh exact-commit review accepts the result.
