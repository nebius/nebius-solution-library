# Open unknowns blocking a backend decision

The machine-readable source is `decision-matrix.v1.json`. All unknowns remain
open; this evidence-index pass resolves none of them.

1. **Matched cohorts:** Kubernetes, plain node VM, and Cerebrium have no
   independently accepted, identical-model/input/hardware-class
   product-boundary cohort.
2. **All ten NIMs:** zero of the required twenty accepted model-arm cells
   (ten BioNeMo NIMs times Arm A and Arm B) are present.
3. **Drain/reclaim:** no exact replacement closes both the missing-items and
   cross-node runtime-authority proof gaps and then passes live H100 A-to-B
   qualification.
4. **Snapshot identity:** no accepted replacement binds every eligible row to
   digest-bearing, topology-correct evidence and a measured conventional
   fallback.
5. **Cost:** no accepted exact-arithmetic model covers per-valid-response,
   failed attempts, billed/free relocation, transfer, warm idle, and matched
   provider receipts.
6. **Cerebrium:** private Nebius placement, provider-boundary security, and the
   first matched cohort are unproven.
7. **Boltz storage:** mutable Task Deck text reported preparation bytes and
   time, but the package contains no raw attempt receipt/source join. The
   reported numbers are non-admissible; both the raw preparation measurement
   and product-boundary result with all request-triggered work after T0 are
   unknown.
8. **Budgets:** product and capacity owners have not ratified any per-scenario
   p50/p95/p99 or per-backend cost ceiling. All current values are null
   placeholders.

Until these exits are independently accepted, the decision matrix must retain
`winner: null`, zero measured matched cohorts, and disabled scoring.
