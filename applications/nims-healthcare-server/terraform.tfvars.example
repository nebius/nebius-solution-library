parent_id    = "project-e00z6b02t8ddk96c49"
kube_context = "nebius-mk8s-forge-eu-e00tjerrz0axkghmbm"
namespace    = "nims-healthcare"

# Keep the real value out of source. Prefer TF_VAR_ngc_key or a local, ignored
# terraform.tfvars file.
ngc_key = "REPLACE_WITH_NGC_API_KEY"

# The current forge-eu cluster has one allocatable GPU per GPU node. Keep this
# false there unless two-GPU nodes are added.
enable_two_gpu_nims = false
